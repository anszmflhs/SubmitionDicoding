import 'package:flutter/material.dart';

class MenuBulanan extends StatelessWidget {
  const MenuBulanan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: Center(
        child: Text("Maaf Belum Ada Menu Bulanan"),
      ),
    );
  }
}
